# Triage Libs

Triage data preprocessing and loading libraries.

## Installation

You can install this library locally using pip. Navigate to the root folder of the repository (where `setup.py` is located) and run:

```bash
pip install .
```

For editable mode (if you want changes in the code to be reflected immediately):

```bash
pip install -e .
```
